## Gap

**Issue:** cnos#684 — "Sigma activation channel — rename `.cn-sigma/logs`, move to symmetric append-only orphan refs (independent stream, off main HEAD)."

**Version / mode:** Mode `explore` (issue frontmatter: `Mode: explore`, `Status: design-first (explore)`). This is a design-first cell, not an implementation cell for orphan refs or history rewriting. The deliverable is a design document + mechanism spec + dry-run migration plan, produced entirely inside the `cnos` repo on `cycle/684`.

**What is not-current about the pre-684 state:** `.cn-sigma/logs/` — dated `YYYYMMDD.md` files under `main`'s tree — is the Sigma foreign→home activation channel, but (a) the name "logs" is too generic for what is specifically an activation-stream, and (b) the channel is an independent communication timeline, not product state, yet it materializes in `main`'s HEAD tree (~1024 commits / ~10 MB, the dominant source of `main` HEAD churn per the operator's dispatch-authorization comment). The v0 convention (`AGENT-ACTIVATION-LOG-v0.md`) also assumes a bare-SHA cursor and a commit-to-`main` attach contract, both of which stop working once the stream moves off `main`.

**What this cycle closes:** the go-forward mechanism design (rename, orphan-ref names + writer/reader roles, amended registration schema, attach-contract sequence, orphan-ref invariants + enforcement, promotion boundary) plus a dry-run-only migration plan and a specified (not executed) content-preservation verification procedure for the already-committed `.cn-sigma/logs/**` history on `main`. It explicitly does **not** close: any physical write to `.cn-sigma/**`, any history rewrite, any ref creation/deletion, any force-push. Those remain operator-executed, separately-gated actions per the operator's binding dispatch-authorization comment (cnos#684, @2026-08-01T07:50:17Z).

**Dispatch chain:** operator authorization (2026-08-01T07:50:17Z, "κ dispatch authorization + bounded scope refinement") → γ R0 scaffold (`.cdd/unreleased/684/gamma-scaffold.md`) → δ inward-membrane enrichment confirming γ's two scope-resolution calls (`.cdd/unreleased/684/gamma-clarification.md`) → this α R0 cycle.

## Skills

**Tier 1 (loaded):**
- `CDD.md` — canonical lifecycle and role contract.
- `src/packages/cnos.cdd/skills/cdd/alpha/SKILL.md` — this role's execution detail; §2.1 dispatch intake, §2.5 self-coherence authoring (incremental, one section per commit — followed in this file), §2.6 pre-review gate, §2.7 request review.
- `issue/SKILL.md` — not separately loaded as a file; AC-boundary interpretation was already resolved by γ (scaffold) and δ (clarification) before dispatch, so α consumed their resolution rather than re-deriving AC boundaries from the raw issue.
- `design/SKILL.md`, `plan/SKILL.md` — **not required, explicit reason:** this cycle's design artifact *is* the deliverable (the convention doc itself), and γ's scaffold already carries the plan-equivalent (AC oracle table, source-of-truth table, α prompt with 4 concrete files, scope guardrails). A separate design/plan artifact preceding the convention doc would duplicate the scaffold rather than add sequencing value for a 4-file, non-code, no-implementation-sequencing cycle.

**Tier 2 (eng/* always-applicable):** not loaded as a distinct bundle — this cycle produces no code, no tests, no CI-affecting artifacts (Markdown + one unexecuted shell script). The shell script (`verify-channel-reconstruction.sh`) was authored with ordinary shell-correctness discipline (`set -euo pipefail`, quoted variables, `mktemp -d` for scratch state) but no `eng/shell` Tier 3 bundle exists in this repo's Tier 2/3 taxonomy to load explicitly; general shell hygiene was applied inline.

**Tier 3 (issue-specific, per γ's scaffold):**
- `delta/SKILL.md` §9.12 (cell/substrate identity boundary, cnos#626) — the binding doctrine constraint that shaped AC6's scope-down (verification procedure specified, not executed) and that forbids any read/write of `.cn-sigma/**` by this cell regardless of checkout visibility. Read in full per the dispatch prompt's explicit instruction.
- `docs/reference/conventions/AGENT-ACTIVATION-LOG-v0.md` — read in full as the doc being superseded; source-of-truth for what v1 must remain consistent with (not contradict/duplicate silently) per the dispatch prompt.
- `gamma-scaffold.md` and `gamma-clarification.md` — both read in full before drafting, per the dispatch prompt's explicit reading list; treated as settled (not re-litigated) per the dispatch instruction.

No skill named in the dispatch prompt or discoverable via `## Related artifacts`-style enumeration was left unread. The issue itself named no `## Related artifacts` section in the strict alpha/SKILL.md §2.1.5 sense (its `## Related` section lists other issues, not artifact paths); all named artifact-path items in `## Related` (`AGENT-ACTIVATION-LOG-v0.md`) were read.

## ACs

Walking the oracle table in `gamma-scaffold.md` §"AC oracle list" one row at a time. File paths below are repo-relative; line numbers reference `docs/reference/conventions/AGENT-ACTIVATION-CHANNEL-v1.md` (the new convention doc) unless stated otherwise.

**AC1 — registration schema declares repo+ref+cursor for both directions; a bare-SHA cursor is rejected.**
Evidence: `AGENT-ACTIVATION-CHANNEL-v1.md` §3 "Registration schema" (L60–98), split per the operator's cursor-ownership amendment into §3.1 "Endpoints (centrally registered)" (L66–82, shared YAML with `repo:`/`ref:` for both `foreign_to_home` and `home_to_foreign`) and §3.2 "Read cursors (writer-local)" (L83–93, a table naming, per direction, the owner and the qualified `{repo, ref, sha}` cursor location — e.g. home's cursor: `cn-sigma:.cn-sigma/state/activations.md` → `last_read_foreign_channel: { repo: ..., ref: ..., cursor: <sha> }`). L91 states explicitly: "Every cursor record carries `repo:` + `ref:` alongside the `<sha>` — the SHA alone does not identify *which* ref it addresses... A cursor entry with a bare SHA and no `repo:`/`ref:` pair fails validation (AC1's falsifiable check)." §7 "Entry format" (L146–172) shows the same qualified-cursor shape in the entry frontmatter (`cursor_in`/`cursor_out` as `{repo, ref, sha}` objects, not bare SHAs), so the bare-SHA-rejection rule is consistent across both the registry-level and entry-level cursor representations. **Satisfied.**

**AC2 — attach contract is fetch-reader-ref / write-and-ff-writer-ref; a writer never pushes the other stream; a reader never writes.**
Evidence: `AGENT-ACTIVATION-CHANNEL-v1.md` §4 "Attach-contract sequence" (L100–121). The six-step loop (fetch declared reader ref → read cursor→reader-tip → do the work → append to writer stream → commit + fast-forward writer ref → record new reader cursor) is stated explicitly, followed by two named sentences: "A writer never pushes the other stream. Step 4/5 only ever target this side's own writer ref..." and "A reader never writes. Step 1/2 are fetch + read only. Consuming the other side's stream never produces a commit on that ref." A per-channel writer/reader table follows (L118–121) naming Sigma-at-cnos vs Sigma-at-home per ref. **Satisfied.**

**AC3 — orphan-ref invariants enforced (single writer, ff-only, no force-push, no-delete-while-registered, cursor reachable) — a non-ff or cross-writer push is rejected.**
Evidence: `AGENT-ACTIVATION-CHANNEL-v1.md` §5 "Orphan-ref invariants and enforcement" (L123–136), a 6-row table (orphaned start, single declared writer, fast-forward only, force-push prohibited, deletion prohibited while registered, cursor-commit-reachable) each paired with a concrete enforcement mechanism (GitHub branch-protection rules on `channels/{agent}/*` for invariants 2–5; a CI/validation check for invariants 1 and 6, since native branch protection has no "must be orphaned" or "cursor reachable" primitive). This is a 1:1 mapping from the issue's Proposal §4 list to enforcement statements, per the oracle's check. **Satisfied.**

**AC4 — promotion rule documented: a channel observation governs current behavior only after promotion to a main-reachable artifact.**
Evidence: `AGENT-ACTIVATION-CHANNEL-v1.md` §6 "Promotion boundary" (L138–144). Binding sentence: "Absent promotion, a channel entry MUST NOT silently govern current behavior. This is Kernel §2.1's no-silent-drops principle applied to the channel..." — uses MUST NOT language, not descriptive prose, per the oracle's check, and names the promotion targets explicitly (issue, CDD contract/design doc, decision record, code change on `main`). **Satisfied.**

**AC5 — rename landed; `.cn-sigma` HEAD retains only current cursors/state + convention README, not the dated stream.**
**Scoped per γ/δ's confirmed split — NOT claimed as fully physically complete.** Evidence: `AGENT-ACTIVATION-CHANNEL-v1.md` §8 "Rename status" (L174–182) states the split explicitly: "Design-landed (this cycle...)" naming §0/§2/§3/§4/§5/§6 as fully specified, versus "Physical-strip deferred (operator-executed, AC7)" naming the `git rm`-of-live-paths action the operator's binding guardrail forbids this cell from performing, and pointing to `dry-run-migration-plan.md` as where that physical step is *planned*, not executed. This mirrors `gamma-scaffold.md`'s own AC5 row and `gamma-clarification.md` §1's confirmation verbatim. **Design-landed; physical-strip explicitly deferred to the operator-executed AC7 step — not claimed as full AC5 completion.**

**AC6 — content preserved before strip — every `.cn-sigma/logs/**` payload currently in `main` history is reconstructable from the orphan ref before any strip is proposed.**
**Procedure specified, not executed by this cell.** Evidence: `.cdd/unreleased/684/verify-channel-reconstruction.sh` — a complete, runnable (by the operator, not by this cell) verification script. Its header (L1–29) states explicitly why this cell cannot run it: `.cn-sigma/` is excluded from this checkout's sparse-checkout (`git sparse-checkout list` → `!/.cn-sigma`, empirically verified this cycle — see below) and `delta/SKILL.md` §9.12 structurally forbids cell-execution cognition from reading/writing `.cn-{agent}/logs/` regardless of checkout visibility. The script's approach (L31–39) uses Git's own content-addressed blob SHAs as the digest mechanism — no bespoke hashing needed — walking `git rev-list --objects main -- .cn-sigma/logs/` for the source set and `git rev-list --objects <orphan-ref>` for the target set, then a set-difference check (`comm -23`). It is referenced from `dry-run-migration-plan.md` §"Sequencing" step 1 as the binding precondition gate before the strip step (§3.3) may run. Command run to confirm exclusion: `git sparse-checkout list` → `/*` then `!/.cn-sigma` (the second line excludes `.cn-sigma` from an otherwise-full checkout), and `ls .cn-sigma` → `No such file or directory` (both re-run this cycle). **Satisfied under the confirmed scope-down (γ scaffold + δ clarification §2): a specified, reviewable verification procedure exists and is correctly sequenced — not executed-with-evidence, which is out of this cell's structural reach.**

**AC7 — dry-run-only rewrite plan: target paths, commit count touched, commits-that-become-empty, SHA-remap blast radius (tags `3.82.x`, open PRs, #682's CDD-by-SHA custody), named as an operator-executed, separately-gated step. Worker performs NO destructive push, history rewrite, force-push, ref deletion, or `git rm` of live paths on `main`.**
Evidence: `.cdd/unreleased/684/dry-run-migration-plan.md`, all six sections. §1 "Target-path scope" (glob `.cn-sigma/logs/**`, explicit exclusion of `docs/development/board/**` with rationale). §3.1 "Analyze first" names `git filter-repo --analyze` as the mechanism for the operator to obtain the *actual* commit count and commits-that-become-empty at execution time (this plan deliberately does not hardcode the operator's ~1024 approximate figure as authoritative, since `main` will have advanced by execution time). §3.2/§3.3 give the `filter-repo`-style command plan for relocate and strip respectively, both explicitly framed as run only against disposable clones, never the live repo. §4 "Blast-radius checklist" covers all three named items — `3.82.x` tags (§4.1, remap-vs-preserve decision named as an operator decision point), open PRs (§4.2, snapshot + commit-map-based rebase strategy + recommended PR-freeze window), and #682's CDD-by-SHA custody (§4.3, named as a hard sequencing dependency — strip gated on #682-side readiness, commit-map preserved as a durable artifact for #682's eventual remap; explicitly not modifying #682's scope). §5 names the rollback ref (`refs/heads/backup/main-pre-cnos684-rewrite`) with naming rationale. §6 "Operator-executed, separately-gated step" restates the no-execution boundary and gives the self-verifiable claim (no rewritten SHAs, no new/moved tags, no force-push on this branch — confirmed via `git tag -l` (empty) and `git diff origin/main...cycle/684 --stat`, both re-run this cycle; see §Self-check below for the actual command output). **Satisfied — plan-only, self-verifiably unexecuted.**

**WRITER_LOCALITY_VIOLATION (negative case) — read cursors are writer-local; never written by the non-owning side. A side attempting to update the other side's read cursor is rejected.**
Evidence: `AGENT-ACTIVATION-CHANNEL-v1.md` §3.1 (L66–82) shows the shared endpoint registry with **no `cursor:` field on either the `foreign_to_home` or `home_to_foreign` entry** — matching the oracle's check that the schema example has no `cursor:` field in the shared block. §3.3 "WRITER_LOCALITY_VIOLATION (negative case)" (L94–98) names the trigger condition concretely ("a side attempts to write or update the read-cursor record that lives on the *other* side's owned surface") and the required response ("MUST reject the write and surface `WRITER_LOCALITY_VIOLATION` as the named error/rejection state"). **Satisfied.**

## Self-check

**Did α's work push ambiguity onto β?** No new ambiguity was introduced beyond what γ/δ already named and resolved. The two load-bearing interpretive calls in this cycle (AC5's design/physical split; AC6's specify-not-execute scope) were made by γ and confirmed by δ *before* this dispatch — α's job was to author artifacts consistent with those confirmed calls, not to re-decide them, and the §ACs section above cites the confirming source (`gamma-clarification.md` §1/§2) rather than re-arguing the point. α did not invent a third scope call of its own; the one place α did exercise independent judgment — filenames (`AGENT-ACTIVATION-CHANNEL-v1.md`, `dry-run-migration-plan.md`, `verify-channel-reconstruction.sh`) and the internal section structure of each doc — was explicitly delegated to α by `gamma-scaffold.md` §"Scope ambiguities flagged for δ" item 3 ("File-naming choices left to α... α has latitude").

**Is every claim backed by evidence in the diff?** Every AC row above cites a specific file + line range (line numbers taken from the actual committed file, re-checked via `grep -n` against the current version, not estimated). The AC6 and AC7 rows additionally cite the actual shell commands run this cycle (`git sparse-checkout list`, `ls .cn-sigma`, `bash -n`, `shellcheck`, `git tag -l`, `git diff origin/main...cycle/684 --stat`) rather than asserting their outcomes from memory — all were re-run immediately before this section was written (see exact output below, §Review-readiness).

**Peer enumeration.** Ran `grep -rl "AGENT-ACTIVATION-LOG-v0" --include="*.md" .` repo-wide (not scoped to `docs/` only, per `alpha/SKILL.md` §2.3's "grep repo-wide by file extension, not by presumed surface category" lesson). Result: beyond the v0 file itself and this cycle's own new/edited artifacts, **7 skill/orchestrator files and 2 doc-papers files** reference `AGENT-ACTIVATION-LOG-v0.md`:
- `src/packages/cnos.core/orchestrators/agent-admin/SKILL.md`, `src/packages/cnos.core/skills/agent/{dispatch-protocol,registration,wake-provider,attach}/SKILL.md`, `src/packages/cnos.cdd/skills/cdd/delta/SKILL.md` (§9.12, read this cycle), `src/packages/cnos.cds/orchestrators/cds-dispatch/SKILL.md`
- `docs/papers/AGENT-MEMORY-LOG-STRUCTURED.md`, `docs/papers/AGENT-COMMS-FUTURES-KISS.md`

**These are NOT updated by this cycle**, and that is a deliberate scope decision, not an oversight: (a) `gamma-scaffold.md`'s binding "Scope guardrails" restrict α's touchable surfaces to exactly the two convention-doc files, the v0 pointer edit, and `.cdd/unreleased/684/` — none of the 9 files above are in that list; (b) substantively, every one of these 9 files references v0's §0 Writer Locality (repo-level) or §0.1 wake-class-ownership mechanics, both of which `AGENT-ACTIVATION-CHANNEL-v1.md` §10 "What changed from v0" states explicitly are **unchanged** by this rename (repo-level Writer Locality carries forward unchanged; wake-class ownership is "orthogonal to which *ref* that surface lives on"). Since the mechanism these 9 files describe has not changed, and the physical storage migration (AC5/AC7) has not landed, these files are not yet stale — they remain accurate descriptions of the *current* on-`main` mechanics. They become a real update surface only once the physical migration (operator-executed, out of this cycle) lands and `.cn-{agent}/logs/` stops being the literal writer-ownership target these files name. Flagged as known debt below rather than silently left off this enumeration, since an initial, narrower `docs/`-only grep undercounted this peer set before the repo-wide re-grep corrected it.

No code, no CI workflow, no schema-bearing parser is touched by this cycle (design-only cell), so the harness-audit and schema-audit gates (`alpha/SKILL.md` §2.4) do not apply — nothing in this repo currently implements the orphan-ref mechanism yet (that implementation is future, separately-dispatched work), so there is no producer/consumer pair of a changed shape to audit.

**Guardrail self-audit (re-run at self-coherence time, not assumed from earlier in the session):**
```
$ git diff origin/main...cycle/684 --stat
 .cdd/unreleased/684/CLAIM-REQUEST.yml              |  18 ++
 .cdd/unreleased/684/dry-run-migration-plan.md      | 159 +++++++++++++++
 .cdd/unreleased/684/gamma-clarification.md         |  15 ++
 .cdd/unreleased/684/gamma-scaffold.md              | 103 ++++++++++
 .cdd/unreleased/684/self-coherence.md              |  ... (this file)
 .cdd/unreleased/684/verify-channel-reconstruction.sh | 137 +++++++++++++
 .../conventions/AGENT-ACTIVATION-CHANNEL-v1.md     | 224 +++++++++++++++++++++
 .../conventions/AGENT-ACTIVATION-LOG-v0.md         |   6 +-
```
No path under `.cn-sigma/**`. No path under `.github/workflows/`. No `docs/development/board/**` path. `git tag -l` → empty (no tags created/moved). Every file in the diff is named explicitly either in this §Self-check paragraph or in §ACs above (CLAIM-REQUEST.yml/gamma-scaffold.md/gamma-clarification.md predate this α cycle and are cited here for completeness of the artifact-enumeration-matches-diff check, `alpha/SKILL.md` §2.6 row 11).

**Caller-path trace for new modules (§2.6 row 12).** `verify-channel-reconstruction.sh` is a new script with no in-repo caller (it is intentionally operator-run, out-of-band, per AC6's scope-down) — its "caller" is named explicitly in `dry-run-migration-plan.md` §"Sequencing" step 1, which is the intended invocation site (a doc reference, not a code call, since this cycle ships no orchestration code that would invoke it programmatically). This is disclosed rather than silently omitted.

## Debt

1. **AC5 physical completion is not landed** — by design, per the confirmed γ/δ scope split. `.cn-sigma` HEAD on `main` still physically carries the dated `.cn-sigma/logs/**` stream; only the design target (this cycle's convention doc) states it should not. Closes only when the operator executes the relocate + verify + strip sequence in `dry-run-migration-plan.md`.
2. **AC6 verification is unexecuted** — by design, per the confirmed δ/§9.12 scope-down. `verify-channel-reconstruction.sh` has been syntax-checked (`bash -n`, `shellcheck`, both clean) but never run against real `.cn-sigma/logs/**` content, since no role in this cell can reach that content. Actual reconstructability evidence lands only when the operator runs it.
3. **9 skill/orchestrator/paper files still cite `AGENT-ACTIVATION-LOG-v0.md`** (enumerated in §Self-check "Peer enumeration" above) and are not updated by this cycle. This is scope-bound, not an oversight — the binding scope guardrails restrict α's touchable surfaces, and substantively these files describe mechanics (repo-level Writer Locality, wake-class ownership) that are unchanged by the rename and remain accurate until the physical migration lands. They become a real update surface at the physical-migration cycle, not this design cycle. Naming this explicitly so a future cycle (or β/δ reviewing this one) does not have to re-discover the same grep.
4. **`cn-sigma`-side registry file is out of this cell's reach.** Per `gamma-scaffold.md` §"Friction notes," the actual `.cn-sigma/state/activations.md` registry file that would carry the amended repo+ref+cursor schema in production lives in the separate `cn-sigma` hub repo. This cycle's convention doc specifies the target shape; landing it in `cn-sigma`'s own registry file is a follow-on action for that repo's owner, not attempted here (would also violate this cell's no-cross-repo-write guardrail).
5. **No Tier 2 `eng/shell` bundle exists to load** for the one shell script this cycle produces — general shell-hygiene discipline (`set -euo pipefail`, quoting, `mktemp -d`) plus `shellcheck`/`bash -n` were applied directly since no formal Tier 2 bundle for shell exists in this repo's skill taxonomy to cite. Not a gap introduced by this cycle, but noted since `alpha/SKILL.md` §2.6 row 9 asks for the toolchain match to be explicit.
6. **Provisional α close-out.** Per `alpha/SKILL.md` §2.8, this dispatch is bounded (α does not spawn β and exits after review-readiness). No `alpha-closeout.md` is written at this stage — the dispatch prompt for this cycle explicitly directs α to stop after `self-coherence.md` is committed/pushed ("Do not spawn β... Your job ends when self-coherence.md is committed, pushed, and you've reported back"), so the standard post-merge re-dispatch close-out path applies later, owned by δ/γ, not by this α session.

## CDD Trace

| Step | Owner | Required output | This cycle |
|---|---|---|---|
| 0 Observe | γ | Selection inputs read | γ read the issue + operator comments; scaffold cites the full source-of-truth table. |
| 1 Select | γ | Named selected gap + decisive rule clause | γ selected cnos#684 (design-first/explore, operator-authorized 2026-08-01T07:50:17Z). |
| 2 Branch | γ | `origin/cycle/684` exists | Verified — branch pre-existed at dispatch (`CLAIM-REQUEST.yml` base), original base `main@daeaa391`. α switched onto it per dispatch instruction, created no new branch. During pre-review gate (§2.6 row 1), `origin/main` was found to have advanced two commits (`6c63758`, `7856c8c` — unrelated board-map/agent-admin churn); α rebased `cycle/684` onto `origin/main` (`git fetch origin main && git rebase origin/main && git push --force-with-lease`), landing new base `main@7856c8c`. Every commit on this branch was replayed with a new SHA by the rebase; all SHA citations in this file were re-stamped to post-rebase values immediately after (§2.6 "SHA citations across path (a) rebase," path (ii) reactive re-stamp). |
| 3 Bootstrap | γ | Issue #684 + scaffold on `cycle/684` | `.cdd/unreleased/684/gamma-scaffold.md` (commit `2155412`) + `gamma-clarification.md` (commit `9f9187b`), both present on `cycle/684`. |
| 4 Gap | α | `self-coherence.md §Gap` | This file, §Gap, commit `90be329`. |
| 5 Mode | α | Mode declaration + Tier 1/2/3 skill set | §Gap (mode: `explore`) + §Skills, commit `a687120`. |
| 6 Artifacts | α | Aligned implementation artifacts on `cycle/684` | Design/docs (no separate design-artifact step — the convention doc *is* the design deliverable, per §Skills' explicit "not required" justification): `docs/reference/conventions/AGENT-ACTIVATION-CHANNEL-v1.md` (commit `938df50`), `docs/reference/conventions/AGENT-ACTIVATION-LOG-v0.md` pointer edit (commit `67fb00a`), `.cdd/unreleased/684/verify-channel-reconstruction.sh` (commit `13833c4`), `.cdd/unreleased/684/dry-run-migration-plan.md` (commit `59575b7`). No tests step (no code produced — design-only cell, justified in §Skills). No separate "code" step (same reason). |
| 7 Self-coherence | α | `self-coherence.md` complete through §CDD Trace | This section, closing the trace. §ACs (commit `a1fe1ee`) maps all 8 oracle rows (AC1–AC7 + WRITER_LOCALITY_VIOLATION) to file+line evidence; §Self-check + §Debt (commit `213334f`) record the corrected repo-wide peer enumeration and 6 named debt items. |

**Artifact-enumeration-matches-diff check (§2.6 row 11), re-verified at trace-close time:**
```
$ git diff origin/main...cycle/684 --stat
 .cdd/unreleased/684/CLAIM-REQUEST.yml                |  18 ++
 .cdd/unreleased/684/dry-run-migration-plan.md        | 159 +++++++++++++++
 .cdd/unreleased/684/gamma-clarification.md           |  15 ++
 .cdd/unreleased/684/gamma-scaffold.md                | 103 ++++++++++
 .cdd/unreleased/684/self-coherence.md                | (this file, growing incrementally)
 .cdd/unreleased/684/verify-channel-reconstruction.sh | 137 +++++++++++++
 .../conventions/AGENT-ACTIVATION-CHANNEL-v1.md       | 224 +++++++++++++++++++++
 .../conventions/AGENT-ACTIVATION-LOG-v0.md           |   6 +-
```
Every path above is named in §ACs, §Self-check, or this table. `CLAIM-REQUEST.yml`/`gamma-scaffold.md`/`gamma-clarification.md` predate α's own commits (γ/δ authored them) but are part of the `origin/main...cycle/684` diff and are named here for completeness.

**Implementation SHA** (last implementation commit before the review-readiness signal commit): `213334f`.
