# γ R0 scaffold — cnos#684

**Issue:** cnos#684 — "Sigma activation channel — rename `.cn-sigma/logs`, move to symmetric append-only orphan refs."
**Mode:** explore / design-first (issue frontmatter: `Mode: explore`, `Status: design-first (explore)`).
**Cycle branch:** `cycle/684`, base `main@daeaa3916ac8c557be5ceb57cda1d0eae11360bf` (verified: `git merge-base main cycle/684` == `git rev-parse main`; branch carries exactly one prior commit, `8238c47` — `CLAIM-REQUEST.yml` only). `run_class: first_pass` per `.cdd/unreleased/684/CLAIM-REQUEST.yml`.
**Dispatch authorization:** operator comment 2026-08-01T07:50:17Z ("κ dispatch authorization + bounded scope refinement") — binding, adds AC6/AC7 and the worker guardrails restated below.

## Work-shape

Substantial / design-first. The deliverable is a **design document + mechanism spec + dry-run migration plan** — not a working orphan-ref implementation, not a history rewrite, not any write to `.cn-sigma/**`. See §Scope guardrails.

## Surfaces α is expected to touch

- `docs/reference/conventions/` — new convention doc (supersedes `AGENT-ACTIVATION-LOG-v0.md`) + a superseded-by pointer added to the v0 doc.
- `.cdd/unreleased/684/` — the dry-run migration plan, the verification-procedure spec/script (source only, not executed against `.cn-sigma/`), and this cycle's own artifacts (`self-coherence.md`, etc.).
- Nothing under `.cn-sigma/**` (repo-relative) — structurally out of reach; see §Scope guardrails and §Friction notes.
- Nothing that force-pushes, rewrites history, deletes refs, or `git rm`s a live path on `main`.

## AC oracle list

Each AC below is restated with a concrete, falsifiable check α/β can run against the produced documents. AC1–AC5 are from the issue body's "Acceptance criteria" section; AC6–AC7 are the operator's binding additions (comment 2); WRITER_LOCALITY_VIOLATION is the operator's binding negative case (comment 1).

| # | AC (as pinned) | Oracle (falsifiable check) |
|---|---|---|
| AC1 | Registration schema declares repo+ref+cursor for **both** directions; a bare-SHA cursor is rejected. | The new convention doc's registration-schema section shows a YAML/schema example with `repo:` + `ref:` + a cursor field for both `foreign_to_home` and `home_to_foreign` channels (per the operator's amended schema in comment 1, endpoints may be centrally registered — repo+ref — but **cursors are writer-local**, not centrally co-located). The spec explicitly states: a registration entry containing only a bare SHA (no `repo:`/`ref:` context) is invalid / must be rejected by any validating tool. Check: grep the doc for a schema block; confirm it has no code path or example that accepts a lone SHA as a complete cursor record. |
| AC2 | Attach contract is fetch-reader-ref / write-and-ff-writer-ref; a writer never pushes the other stream; a reader never writes. | The doc states the attach-loop steps explicitly (fetch declared reader ref → read cursor→reader-tip → append to writer stream → commit + fast-forward writer ref → record new reader cursor) and names, for each of the two channels, which side is writer and which is reader. Check: the doc contains a step-by-step attach sequence naming both roles per channel, and an explicit sentence ruling out "writer pushes to reader's ref" and "reader commits to its own ref." |
| AC3 | Orphan-ref invariants enforced (single writer, ff-only, no force-push, no-delete-while-registered, cursor reachable) — a non-ff or cross-writer push is rejected. | The mechanism spec lists all five invariants explicitly (orphaned start, single declared writer, fast-forward only, force-push prohibited, delete-while-registered prohibited, cursor-commit-reachable-from-ref) and names the enforcement mechanism (e.g., GitHub branch protection rules on `refs/heads/channels/sigma/*`, or a CI check) concretely enough that δ/operator could configure it without re-deriving the invariant from prose. Check: each of the 5 named invariants in the issue's Proposal §4 has a 1:1 corresponding enforcement statement in the spec. |
| AC4 | Promotion rule documented: a channel observation governs current behavior only after promotion to a main-reachable artifact. | The doc contains an explicit "Promotion boundary" section restating: a channel entry is a communication record only; it becomes project memory/authority only via promotion into an issue / CDD contract / decision / code change on `main`; absent promotion, the entry MUST NOT silently govern current behavior (cite Kernel §2.1 no-silent-drops). Check: section exists, uses "MUST NOT... without promotion" or equivalent binding language, not merely descriptive prose. |
| AC5 | Rename landed; `.cn-sigma` HEAD retains only current cursors/state + convention README, not the dated stream. | **Scoped for this cycle** (see the scope note directly below): the *design* of the rename is landed — new convention doc adopts "activation channel" naming (not "logs"), the two orphan-ref names are the canonical target paths, and the go-forward mechanism (attach-contract) specifies that *new* entries target the orphan refs rather than `.cn-sigma/logs/YYYYMMDD.md`. The *physical* fact "`.cn-sigma` HEAD retains only current cursors/state ... not the dated stream" requires removing already-committed content from `main`'s current tree — the operator's guardrail explicitly forbids the worker from doing this (`NO git rm of live paths on main`). Full AC5 completion is therefore **necessarily split across this cycle (design landed) and the operator-executed step named in AC7 (physical strip)**. α's R0 self-coherence MUST name this split explicitly rather than claiming full AC5 completion. δ/operator should be aware this is a scope-resolution γ is making, not a silent redefinition — flagged below in §Scope ambiguities. |
| AC6 | Content preserved before strip — every `.cn-sigma/logs/**` payload currently in `main` history is reconstructable from the orphan ref before any strip is proposed. | **α cannot execute this verification directly** — `.cn-sigma/` is excluded from this repo's sparse-checkout (`git sparse-checkout list` shows `!/.cn-sigma`) and `delta/SKILL.md` §9.12 structurally forbids δ/γ/α/β from reading or writing `.cn-{agent}/logs/` regardless of checkout visibility. α's deliverable for AC6 is therefore a **specified, reviewable verification procedure/script** (e.g. a script that walks `.cn-sigma/logs/**` history via `git log --follow`/`git show`, computes a content digest per file, imports the same content into the orphan ref, and diffs) that the operator (or an actor with `.cn-sigma/` access) runs as a precondition gate before AC7's strip step. Check: the script/procedure exists, is complete enough to run without further design input, and is explicitly sequenced *before* any strip action in the dry-run plan (AC7). Execution + evidence of an actual successful reconstruction is out of this cycle's closure scope — it lands when the operator runs the gated step. |
| AC7 | Dry-run-only rewrite plan: target paths, commit count touched, commits-that-become-empty, SHA-remap blast radius (tags `3.82.x`, open PRs, #682's CDD-by-SHA custody), named as an operator-executed, separately-gated step. Worker performs NO destructive push, history rewrite, force-push, ref deletion, or `git rm` of live paths on `main`. | A dry-run migration plan document exists naming: (a) exact target path glob (`.cn-sigma/logs/**` only — `docs/development/board/**` explicitly excluded per the scoping note); (b) the `git filter-repo`-style command(s) that *would* run (as a plan, not executed); (c) a placeholder/instructions for how the operator obtains the actual commit count touched and commits-that-become-empty (e.g. "operator runs `git filter-repo --analyze` first"); (d) a named blast-radius checklist covering `3.82.x` tags, open PRs, and #682's CDD-by-SHA custody mechanism, each with a stated remap/re-anchor strategy; (e) an explicit statement that the actual rewrite + force-push is a separate, operator-executed, separately-gated step with a rollback ref (pre-rewrite backup of `main`) named. Check: the plan document contains no evidence of having actually been executed (no new tags, no rewritten SHAs, no force-push) — verifiable by `git log main` / `git tag` showing no unexpected mutation. |
| WRITER_LOCALITY_VIOLATION (negative case, operator comment 1) | Read cursors are writer-local; never written by the non-owning side. A side attempting to update the other side's read cursor is rejected. | The registration-schema section (post-amendment, per comment 1) shows: (a) *endpoints* (repo+ref, both directions) may live in one shared registry; (b) *read cursors* are stored only on the surface the reader owns — home stores the foreign→home read cursor in home's own registry/state; foreign stores the home→foreign read cursor in foreign's own writer-stream/current-state; (c) an explicit named error/rejection state `WRITER_LOCALITY_VIOLATION` fires when a side attempts to write to the other side's read-cursor location. Check: the doc's schema example matches comment 1's YAML shape (endpoints-only in the shared registry, no `cursor:` field on either `foreign:`/`home:` entry in the shared block), and a dedicated subsection names the `WRITER_LOCALITY_VIOLATION` case with a concrete trigger condition. |

## Source-of-truth table

| Claim / surface | Authoritative source |
|---|---|
| Current (pre-684) convention: two-artifact, single-writer, append-only; bare-SHA cursor model; entry format; §0 Writer Locality invariant; §6 branch discipline (main-only channel surfaces) | `docs/reference/conventions/AGENT-ACTIVATION-LOG-v0.md` (read in full for this scaffold) — this is the doc the new convention supersedes. |
| §0.1 wake-class writer ownership — dispatch wakes (`cds-dispatch` and future `cdr-dispatch`/`cdw-dispatch`) MUST NOT write `.cn-{agent}/logs/`; admin wake is sole writer | `AGENT-ACTIVATION-LOG-v0.md` §0.1 — directly explains why this cell's own dispatch context (a `cds-dispatch` wake firing) is not the right actor to physically write channel content even if it could. |
| Structural exclusion: cell-execution cognition (δ/γ/α/β under wake-invoked-δ) does not read or write `.cn-{agent}/logs/`, regardless of checkout | `delta/SKILL.md` §9.12 "cell/substrate identity boundary" (cnos#626) |
| Target architecture: symmetric orphan refs, repo+ref+cursor schema, attach-contract change, orphan-ref invariants, promotion rule, AC1–AC5, non-goals, scope in/out | cnos#684 issue body |
| Cursor-ownership amendment: read cursors are writer-local; supersedes the inline `foreign:{cursor}`/`home:{cursor}` example in Proposal §2; WRITER_LOCALITY_VIOLATION negative case | cnos#684 comment @2026-08-01T06:59:04Z ("Pre-dispatch contract amendment — cursor ownership") |
| AC6/AC7; relocate-then-strip decision; worker scope bound to design+mechanism+dry-run-plan only; explicit no-rewrite/no-force-push/no-ref-deletion/no-git-rm guardrails; `.cn-sigma/logs/**`-only scoping (excludes `docs/development/board/**`) | cnos#684 comment @2026-08-01T07:50:17Z ("κ dispatch authorization + bounded scope refinement") — **binding** |
| CDD-by-SHA custody mechanism that AC7's blast-radius analysis must account for (cross-reference only — out of scope to modify in this cycle) | cnos#682 "architecture: dematerialize closed CDD cells from HEAD..." (OPEN; sibling plane, not this cell's scope) |
| `.cn-sigma/` is structurally absent from this cycle's working tree | `git sparse-checkout list` → `/*` then `!/.cn-sigma` (empirically verified during this scaffold's authoring) |
| Cycle provenance: `run_class: first_pass`, base SHA, claim reasoning | `.cdd/unreleased/684/CLAIM-REQUEST.yml` |
| Wake-invoked-δ routing / R0 artifact contract this scaffold satisfies | `delta/SKILL.md` §9.3 step 1, §9.5 (R0 row) |

## α prompt

**Branch:** `cycle/684`. `git switch cycle/684` (do not create a new branch).

**Task.** Under cnos#684 (design-first / explore cell), produce the go-forward mechanism design for the Sigma activation channel plus a dry-run-only migration plan for the existing on-`main` `.cn-sigma/logs/**` history. This is **not** an implementation cycle for orphan refs or history rewriting — it is a design + spec + plan cycle. Walk every AC in the oracle table above (AC1–AC7 + WRITER_LOCALITY_VIOLATION) and produce artifacts that satisfy each oracle's falsifiable check.

**Concrete files to write:**

1. A new convention doc at `docs/reference/conventions/` (naming is α's call, e.g. `AGENT-ACTIVATION-CHANNEL-v1.md`) that supersedes `AGENT-ACTIVATION-LOG-v0.md`. It must cover: the rename rationale (channel, not "logs"); the two orphan-ref names and their per-direction writer/reader roles; the amended registration schema (endpoints centrally registered, cursors writer-local per comment 1 — AC1, WRITER_LOCALITY_VIOLATION); the attach-contract sequence (AC2); the orphan-ref invariants and their enforcement mechanism (AC3); the promotion-boundary rule (AC4); and an explicit "Rename status" section stating design-landed vs physically-landed per the AC5 scope split above.
2. A superseded-by pointer added to the top of `AGENT-ACTIVATION-LOG-v0.md` (a short frontmatter/prose note; do not delete or gut the file — it remains the historical record of v0 and the source doc for what's being superseded).
3. A dry-run migration plan at `.cdd/unreleased/684/` (α's naming call, e.g. `dry-run-migration-plan.md`) satisfying AC7: target-path scope, the `filter-repo`-style command plan, blast-radius checklist (tags `3.82.x`, open PRs, cnos#682's CDD-by-SHA custody), rollback-ref naming, and the explicit "operator-executed, separately-gated step" framing.
4. A verification procedure/script satisfying AC6, referenced from the migration plan, sequenced as a precondition gate before any strip step. Write the script's *source* (it is safe to author without executing it against `.cn-sigma/`, since authoring only requires knowing the git commands, not reading current `.cn-sigma/` content).

**Constraints (binding — see §Scope guardrails below; do not improvise past them).**

**Do not spawn β.** Write `.cdd/unreleased/684/self-coherence.md §R0` per `alpha/SKILL.md`, naming AC-by-AC verification against the oracle table above (including the explicit AC5/AC6 scope-split language), append the review-readiness signal, commit, push, and exit.

## β prompt

**Branch:** `cycle/684`. `git switch cycle/684`.

**Task.** Independently verify α's R0 self-coherence against the AC oracle list in this scaffold (§AC oracle list) — walk AC1 through AC7 plus WRITER_LOCALITY_VIOLATION, one at a time, and separately verify the scope guardrails below were honored (this is a hard gate, not a normal finding — a violation here is D-severity regardless of doc quality).

**Verification checklist (in addition to the per-AC oracle checks above):**

- `git diff main...cycle/684 --stat` touches **no** path under `.cn-sigma/**`.
- `git diff main...cycle/684 --stat` touches **no** `.github/workflows/*`, no ref, no tag.
- No commit on `cycle/684` performs a force-push-shaped operation (not directly checkable from a normal branch diff, but confirm no rewritten-history markers, e.g. no `git filter-repo` invocation actually ran — the migration-plan doc should describe commands, not contain evidence of having run them).
- `docs/development/board/**` is untouched (scoping note: board history is explicitly excluded from this cell's strip scope; if α's plan mentions board history at all, it must state exclusion, not inclusion).
- AC5's self-coherence claim is the split claim (design-landed / physical-strip-deferred-to-AC7), not a claim of full physical completion.
- AC6's self-coherence claim is "verification procedure specified, not executed by this cell" — not a claim of executed verification evidence.
- The new convention doc actually supersedes (references, doesn't silently duplicate/conflict with) `AGENT-ACTIVATION-LOG-v0.md`, and `AGENT-ACTIVATION-LOG-v0.md` still exists (not deleted).

**Do not spawn α.** Write `.cdd/unreleased/684/beta-review.md §R0` with verdict + findings (if any), commit, push, and exit. If verdict is `iterate`, findings route back to α at R1 via δ.

## Scope guardrails (restated verbatim from the operator's binding comment — α MUST NOT drift past these)

- The worker (α, this cycle) performs **NO** destructive push, **NO** history rewrite, **NO** force-push, **NO** ref deletion, and **NO** `git rm` of live paths on `main`.
- Deliverables are **design + mechanism + dry-run plan only**. The actual `git filter-repo`-style rewrite, and any physical creation/population of `refs/heads/channels/sigma/*`, is an **operator-executed, separately-gated step**, explicitly out of this cell's scope.
- Target for the history-migration plan is **`.cn-sigma/logs/**` only**. `docs/development/board/**` history is **explicitly excluded** from any strip proposal in this cell (per the operator's scoping note — board is kept current-state per #681, already debounced).
- α does not touch `.cn-sigma/**` at all in this cycle — not for reading, not for writing — both because it is excluded from the working tree's sparse-checkout and because `delta/SKILL.md` §9.12 structurally forbids it for wake-invoked cell-execution cognition regardless of checkout state.
- This cell does not fold into or modify cnos#682's scope (the CDD-by-SHA ancestry plane) — it only names #682's custody mechanism as a blast-radius item AC7's plan must account for.
- No cross-repo write of any kind (writer locality, `AGENT-ACTIVATION-LOG-v0.md` §0) — this cell's α/β operate entirely inside the `cnos` repo on `cycle/684`; nothing is written to `cn-sigma`.

## Scope ambiguities flagged for δ

1. **AC5 vs. the AC6/AC7 worker guardrails is a genuine internal tension in the pinned issue text**, not something γ can resolve by re-reading more carefully — AC5 literally requires `.cn-sigma` HEAD to no longer carry the dated stream, while the operator's binding second comment forbids the worker from performing the `git rm`/history action that would produce that state. γ has resolved this by splitting AC5 into a design-landed component (in scope this cycle) and a physical-strip component (deferred to the operator-executed AC7 step). This is the single most load-bearing interpretive call in this scaffold; δ/operator should confirm it rather than have it discovered as a surprise at β review or close-out.
2. **AC6's "verification" cannot be executed inside this cell.** Per `delta/SKILL.md` §9.12 and the sparse-checkout exclusion, no role in this cell can read `.cn-sigma/logs/` to actually prove reconstructability. γ has scoped AC6 down to "a specified, reviewable verification procedure exists and is correctly sequenced" rather than "verification executed with evidence." If δ or the operator wants actual executed-verification evidence to land in this cycle, that would require an explicit override of §9.12's structural exclusion (β prompt above treats any α attempt to touch `.cn-sigma/**` as a hard-gate violation, so this ambiguity must be resolved before dispatch, not discovered mid-cycle).
3. **File-naming choices left to α** (the new convention doc's exact filename, the dry-run plan's exact filename) are intentionally not pinned — γ named the required *content* and a suggested location; α has latitude on exact filenames as long as `docs/reference/conventions/` and `.cdd/unreleased/684/` are the respective homes and the files are discoverable/named descriptively.

## Friction notes

- **`.cn-sigma/` visibility gap.** This working tree's sparse-checkout config (`/* ` then `!/.cn-sigma`) excludes `.cn-sigma/` entirely — `ls .cn-sigma` returns "No such file or directory" even though the path is presumably populated on `main` HEAD in a full checkout. γ could not read the current `.cn-sigma/logs/**` content, the `.cn-sigma/state/activations.md` registry shape, or any `.cn-sigma/spec/` convention docs referenced by `AGENT-ACTIVATION-LOG-v0.md`'s `related:` frontmatter (e.g. `cn-sigma:.cn-sigma/spec/OPERATOR.md § Activation logs`) — those live in the separate `cn-sigma` hub repo, not this repo, and were not fetched. This scaffold is built entirely from `AGENT-ACTIVATION-LOG-v0.md` (this repo, fully readable) plus the issue text; it does not assume any undocumented specifics about current `.cn-sigma/logs/` file counts, shapes, or edge cases beyond what the operator's comment 2 states (~1024 commits / ~10 MB under `.cn-sigma/logs/**`).
- **This exclusion is doctrine, not accident** — confirmed by `delta/SKILL.md` §9.12 (cnos#626), which independently forbids cell-execution cognition from reading/writing `.cn-{agent}/logs/` regardless of checkout visibility. The sparse-checkout config and the doctrine are mutually reinforcing, not a gap to "fix" — α should not request broader checkout access as a workaround.
- **`.cn-sigma` state/registry file locations named in the issue's target schema** (e.g. where `.cn-sigma:.cn-sigma/state/activations.md` would carry the amended repo+ref+cursor registry per comment 1) live in the `cn-sigma` hub repo, a separate repo from `cnos`. This cell's deliverables live entirely in `cnos`; any change to the `cn-sigma`-side registry file is out of this cell's reach (different repo, different push permission) and should be named in the new convention doc as a follow-on action for whoever owns that repo, not attempted here.
