---
ts: 2026-08-05T17:55:00Z
from:
  agent: usurobor/cn-sigma
  locus: usurobor/cnos
rank: r0
class: rca
---

# cn-sigma@cnos — 2026-08-05

Two entries appended during the day and sealed together. Both bodies are
verbatim as first written; only the containers were merged, to conform to
one r0 document per locus per UTC day.

---

## 17:55Z

## Lesson: review authority belongs on the PR; agent identity is unverifiable under one GitHub account

**Context.** During cn-pi PR #1 review, verdicts were posted as `class: review` messages on dialogue refs (events 11–13). Operator corrected the channel.

**Two durable lessons:**

1. **Review = project authority → project-native artifact (the PR), not dialogue.** Per cnos#698's own boundary, a code review that gates a merge is project authority and belongs in a project-native artifact — an issue, an **Architecture Decision Record (ADR)**, a Coherence-Driven Development (CDD) receipt, a spec, a reviewed PR, or a commit — never in a communication-only stream. Dialogue carries only the *request* and the *notification/pointer*.

2. **Identity is unverifiable under a single GitHub account.** All agents (cn-sigma, cn-pi, cn-omega) run under one account (`usurobor`). GitHub's identity is the account, so it cannot distinguish agents: native review gating (APPROVE / REQUEST_CHANGES) is unavailable cross-agent, and a ref name or `from:` field is a **claim, not proof** — anything with push access can write any agent's ref. Current `registered-repo` trust mode = convention, not authorship.

**Fix direction (signed-activation, cnos#698's second trust mode).** Give each activation a distinct signing key (SSH commit signing = KISS); bind agent → public key in `peers.yaml`/`activations.yaml` (`allowed_signers`); verify each commit's signature against the ref-owner's registered key on the pull / merge / compaction path. This yields cryptographic authorship independent of the GitHub account. The PR-review gate is the trigger to move off `registered-repo`.

**Evidence:** GitHub returned "cannot request changes on your own pull request" for cn-pi#1 (same-account). Review re-posted as a review comment; merge gate stays operator-honored, not GitHub-enforced.

---

## 19:30Z

## Lesson: verify a review finding against rendered/executed reality before posting — never from an isolated grep or isolated-function call

**Trigger:** reviewing code or docs and about to flag a defect.

**Failure, twice in one session:**
1. **cn-pi PR #1 "D1-residual"** — claimed the parser accepts an identity-less `from: {}`. Tested `parse_dialogue_frontmatter` (the tokenizer) *in isolation*; the real gate `validate_dialogue_event` rejects it before any Git write. Blocker was false.
2. **cnos PR #703 fencing** — claimed `## Decision/Problem/Proposal` leak as document headings. Used a **fence-unaware `grep '^##'`**; the lines are inside `\`\`\`yaml` code fences and render as code. Finding was false.

**Root cause:** flagging from a narrow probe (one function, one grep) instead of the reality a reader/runtime actually experiences (full pipeline; fence-aware render).

**Rule (imperative):**
- For **code** findings: exercise the **full path** a real input takes (caller → validator → mutation), not one function in isolation. Reproduce the actual harm end-to-end.
- For **markdown/doc** findings: verify **fence-aware** (does it render as a heading, or is it inside a code block?), not via raw line-grep.
- Only post a finding after reproducing the harm as the reader/runtime would see it. A grep or isolated call is a *hint to investigate*, never evidence to flag on.

**Payoff:** independent review earns trust only when its findings survive verification. Two false blockers cost credibility and churn; both were caught only on a second, proper check.
