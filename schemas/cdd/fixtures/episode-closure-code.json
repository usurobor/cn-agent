{
  "closure_schema": "cnos.cellkernel.episode-closure.v0",
  "protocol_validated": false,
  "status": "accepted",
  "decision": "accept",
  "verdict": {
    "pass": true
  },
  "receipt": {
    "record": {
      "canon": "cnos.cellkernel.episode-record-canon.v0",
      "episode_id": "ep-f30c134ac40618552b133e9818b2b08a",
      "execution_mode": "cognitive",
      "resolved_spec": {
        "version": "cnos.cellspec.v0",
        "declared_protocol": "cnos.cdd.cds.receipt.v1",
        "profile": "code",
        "params": {
          "base_sha": "HEAD",
          "language": "go",
          "provider": "claude",
          "repo": "."
        },
        "alpha_skills": [
          "eng",
          "go"
        ],
        "beta_skills": [
          "go",
          "cds-review"
        ]
      },
      "contract": {
        "id": "cds-code-real",
        "goal": "src/go/internal/cellwork has no tests. Add src/go/internal/cellwork/cellwork_test.go covering Materialize and Worktree.Diff: a worktree is created at the resolved base SHA, Diff reports a created file, Diff is empty when nothing changed, an unresolvable base fails, and a non-repository path fails. Build the fixture repo with git commands in a t.TempDir(). Match the style of the existing tests in src/go/internal/cellcog/code_test.go. Do not modify any other file.",
        "required_evidence": [
          {
            "id": "diff",
            "kind": "diff",
            "producer": "alpha"
          }
        ]
      },
      "alpha": {
        "execution_id": "alpha-f25d384025d786e8f25d9c9ed69536a6",
        "artifacts": [
          {
            "id": "base_sha",
            "kind": "sha",
            "encoding": "utf8",
            "text": "61a5e93f510a78ea4dea8d6e54b6987eef422e64"
          },
          {
            "id": "diff",
            "kind": "diff",
            "encoding": "utf8",
            "text": "diff --git a/src/go/internal/cellwork/cellwork_test.go b/src/go/internal/cellwork/cellwork_test.go\nnew file mode 100644\nindex 00000000..07d390c1\n--- /dev/null\n+++ b/src/go/internal/cellwork/cellwork_test.go\n@@ -0,0 +1,96 @@\n+package cellwork\n+\n+import (\n+\t\"context\"\n+\t\"os\"\n+\t\"os/exec\"\n+\t\"path/filepath\"\n+\t\"strings\"\n+\t\"testing\"\n+)\n+\n+// testRepo builds a one-commit git repository and returns its path and HEAD.\n+func testRepo(t *testing.T) (string, string) {\n+\tt.Helper()\n+\tdir := t.TempDir()\n+\trun := func(args ...string) string {\n+\t\tt.Helper()\n+\t\tcmd := exec.Command(\"git\", args...)\n+\t\tcmd.Dir = dir\n+\t\tcmd.Env = append(os.Environ(),\n+\t\t\t\"GIT_AUTHOR_NAME=t\", \"GIT_AUTHOR_EMAIL=t@t\", \"GIT_COMMITTER_NAME=t\", \"GIT_COMMITTER_EMAIL=t@t\")\n+\t\tout, err := cmd.CombinedOutput()\n+\t\tif err != nil {\n+\t\t\tt.Fatalf(\"git %s: %v\\n%s\", strings.Join(args, \" \"), err, out)\n+\t\t}\n+\t\treturn strings.TrimSpace(string(out))\n+\t}\n+\trun(\"init\", \"-q\", \"-b\", \"main\")\n+\tif err := os.WriteFile(filepath.Join(dir, \"README.md\"), []byte(\"base\\n\"), 0o600); err != nil {\n+\t\tt.Fatal(err)\n+\t}\n+\trun(\"add\", \"-A\")\n+\trun(\"commit\", \"-qm\", \"base\")\n+\treturn dir, run(\"rev-parse\", \"HEAD\")\n+}\n+\n+// Materialize cuts a worktree at the resolved base SHA, not at a moving name.\n+func TestMaterializeResolvesBase(t *testing.T) {\n+\trepo, head := testRepo(t)\n+\twt, release, err := Materialize(context.Background(), repo, \"HEAD\")\n+\tif err != nil {\n+\t\tt.Fatalf(\"materialize: %v\", err)\n+\t}\n+\tdefer release()\n+\tif wt.BaseSHA != head {\n+\t\tt.Fatalf(\"base sha = %q, want resolved HEAD %q\", wt.BaseSHA, head)\n+\t}\n+\tif _, err := os.Stat(filepath.Join(wt.Dir, \"README.md\")); err != nil {\n+\t\tt.Fatalf(\"worktree missing base content: %v\", err)\n+\t}\n+}\n+\n+// Diff reports a file the seat created, and nothing when the seat changed\n+// nothing — the caller must not manufacture evidence from an empty diff.\n+func TestDiffReportsCreatedFileAndIsEmptyWhenUnchanged(t *testing.T) {\n+\trepo, _ := testRepo(t)\n+\twt, release, err := Materialize(context.Background(), repo, \"HEAD\")\n+\tif err != nil {\n+\t\tt.Fatalf(\"materialize: %v\", err)\n+\t}\n+\tdefer release()\n+\n+\tempty, err := wt.Diff(context.Background())\n+\tif err != nil {\n+\t\tt.Fatalf(\"diff of untouched worktree: %v\", err)\n+\t}\n+\tif empty != \"\" {\n+\t\tt.Fatalf(\"diff of untouched worktree = %q, want empty\", empty)\n+\t}\n+\n+\tif err := os.WriteFile(filepath.Join(wt.Dir, \"NOTES.md\"), []byte(\"hello\\n\"), 0o600); err != nil {\n+\t\tt.Fatal(err)\n+\t}\n+\tdiff, err := wt.Diff(context.Background())\n+\tif err != nil {\n+\t\tt.Fatalf(\"diff: %v\", err)\n+\t}\n+\tif !strings.Contains(diff, \"NOTES.md\") || !strings.Contains(diff, \"+hello\") {\n+\t\tt.Fatalf(\"diff does not report the created file: %q\", diff)\n+\t}\n+}\n+\n+// An unresolvable base fails before any worktree is cut.\n+func TestMaterializeUnresolvableBaseFails(t *testing.T) {\n+\trepo, _ := testRepo(t)\n+\tif _, _, err := Materialize(context.Background(), repo, \"no-such-rev\"); err == nil {\n+\t\tt.Fatal(\"an unresolvable base must fail\")\n+\t}\n+}\n+\n+// A non-repository path fails closed rather than silently doing nothing.\n+func TestMaterializeNonRepositoryFails(t *testing.T) {\n+\tif _, _, err := Materialize(context.Background(), t.TempDir(), \"HEAD\"); err == nil {\n+\t\tt.Fatal(\"a non-repository path must fail\")\n+\t}\n+}\n"
          }
        ]
      },
      "matter": {
        "data": "diff --git a/src/go/internal/cellwork/cellwork_test.go b/src/go/internal/cellwork/cellwork_test.go\nnew file mode 100644\nindex 00000000..07d390c1\n--- /dev/null\n+++ b/src/go/internal/cellwork/cellwork_test.go\n@@ -0,0 +1,96 @@\n+package cellwork\n+\n+import (\n+\t\"context\"\n+\t\"os\"\n+\t\"os/exec\"\n+\t\"path/filepath\"\n+\t\"strings\"\n+\t\"testing\"\n+)\n+\n+// testRepo builds a one-commit git repository and returns its path and HEAD.\n+func testRepo(t *testing.T) (string, string) {\n+\tt.Helper()\n+\tdir := t.TempDir()\n+\trun := func(args ...string) string {\n+\t\tt.Helper()\n+\t\tcmd := exec.Command(\"git\", args...)\n+\t\tcmd.Dir = dir\n+\t\tcmd.Env = append(os.Environ(),\n+\t\t\t\"GIT_AUTHOR_NAME=t\", \"GIT_AUTHOR_EMAIL=t@t\", \"GIT_COMMITTER_NAME=t\", \"GIT_COMMITTER_EMAIL=t@t\")\n+\t\tout, err := cmd.CombinedOutput()\n+\t\tif err != nil {\n+\t\t\tt.Fatalf(\"git %s: %v\\n%s\", strings.Join(args, \" \"), err, out)\n+\t\t}\n+\t\treturn strings.TrimSpace(string(out))\n+\t}\n+\trun(\"init\", \"-q\", \"-b\", \"main\")\n+\tif err := os.WriteFile(filepath.Join(dir, \"README.md\"), []byte(\"base\\n\"), 0o600); err != nil {\n+\t\tt.Fatal(err)\n+\t}\n+\trun(\"add\", \"-A\")\n+\trun(\"commit\", \"-qm\", \"base\")\n+\treturn dir, run(\"rev-parse\", \"HEAD\")\n+}\n+\n+// Materialize cuts a worktree at the resolved base SHA, not at a moving name.\n+func TestMaterializeResolvesBase(t *testing.T) {\n+\trepo, head := testRepo(t)\n+\twt, release, err := Materialize(context.Background(), repo, \"HEAD\")\n+\tif err != nil {\n+\t\tt.Fatalf(\"materialize: %v\", err)\n+\t}\n+\tdefer release()\n+\tif wt.BaseSHA != head {\n+\t\tt.Fatalf(\"base sha = %q, want resolved HEAD %q\", wt.BaseSHA, head)\n+\t}\n+\tif _, err := os.Stat(filepath.Join(wt.Dir, \"README.md\")); err != nil {\n+\t\tt.Fatalf(\"worktree missing base content: %v\", err)\n+\t}\n+}\n+\n+// Diff reports a file the seat created, and nothing when the seat changed\n+// nothing — the caller must not manufacture evidence from an empty diff.\n+func TestDiffReportsCreatedFileAndIsEmptyWhenUnchanged(t *testing.T) {\n+\trepo, _ := testRepo(t)\n+\twt, release, err := Materialize(context.Background(), repo, \"HEAD\")\n+\tif err != nil {\n+\t\tt.Fatalf(\"materialize: %v\", err)\n+\t}\n+\tdefer release()\n+\n+\tempty, err := wt.Diff(context.Background())\n+\tif err != nil {\n+\t\tt.Fatalf(\"diff of untouched worktree: %v\", err)\n+\t}\n+\tif empty != \"\" {\n+\t\tt.Fatalf(\"diff of untouched worktree = %q, want empty\", empty)\n+\t}\n+\n+\tif err := os.WriteFile(filepath.Join(wt.Dir, \"NOTES.md\"), []byte(\"hello\\n\"), 0o600); err != nil {\n+\t\tt.Fatal(err)\n+\t}\n+\tdiff, err := wt.Diff(context.Background())\n+\tif err != nil {\n+\t\tt.Fatalf(\"diff: %v\", err)\n+\t}\n+\tif !strings.Contains(diff, \"NOTES.md\") || !strings.Contains(diff, \"+hello\") {\n+\t\tt.Fatalf(\"diff does not report the created file: %q\", diff)\n+\t}\n+}\n+\n+// An unresolvable base fails before any worktree is cut.\n+func TestMaterializeUnresolvableBaseFails(t *testing.T) {\n+\trepo, _ := testRepo(t)\n+\tif _, _, err := Materialize(context.Background(), repo, \"no-such-rev\"); err == nil {\n+\t\tt.Fatal(\"an unresolvable base must fail\")\n+\t}\n+}\n+\n+// A non-repository path fails closed rather than silently doing nothing.\n+func TestMaterializeNonRepositoryFails(t *testing.T) {\n+\tif _, _, err := Materialize(context.Background(), t.TempDir(), \"HEAD\"); err == nil {\n+\t\tt.Fatal(\"a non-repository path must fail\")\n+\t}\n+}\n"
      },
      "beta": {
        "execution_id": "beta-74c5743a0fe7dd0a33934941d167afed",
        "artifacts": []
      },
      "review": {
        "pass": true,
        "notes": "mechanical review only: matter carries 3223 bytes of content; a mechanical seat cannot judge whether it meets the goal"
      }
    },
    "scope_lift_digest": "e1e17c6c152b9384bf6f8513ca8167c93fd54309469ad7b9c7f1fbeb8b290cb1"
  }
}
