{
  "contract": {
    "id": "cnos-cell-demo",
    "goal": "Add retry with exponential backoff to internal/dispatch.Backend",
    "required_evidence": [
      { "id": "diff", "kind": "diff" },
      { "id": "beta_review", "kind": "review" }
    ]
  },
  "protocol_id": "cnos.cdd.cds.receipt.v1",
  "params": {
    "language": {
      "kind": "skill",
      "required": true,
      "domain": ["go", "ocaml", "rust", "python", "typescript"]
    },
    "style": { "kind": "skill", "required": false, "default": "functional" }
  },
  "alpha": { "skills": ["eng", "$language", "$style"] },
  "beta": { "skills": ["$language", "cds-review"] },
  "budget": { "tokens": 200000 }
}
