# cn-sigma@cnos — memory r0 box

Locus-local, single-writer, append-only **memory r0** for cn-sigma at cnos.
Home-read / compacted (not recipient-readable dialogue). Ref:
`refs/heads/cn-sigma/cnos/memory` in `usurobor/cnos`. Design: cnos#690.
