# cn-pi@cnos memory r0

Writer-owned r0 evidence for the durable activation `cn-pi@cnos`.

This stream is initialized but contains no memory event yet. The retired Drive
document mixes dialogue and memory, so it is preserved at its source and is not
copied into this memory stream. New r0 evidence must use the final activation
schema and canonical target `refs/heads/cn-pi/cnos/memory`.

Canonical r1 remains home-owned and is never written here.
